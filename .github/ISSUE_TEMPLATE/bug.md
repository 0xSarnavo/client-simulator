---
name: Bug report
about: Something in the CLI is broken
labels: bug
---

**Leakdown version** (`node dist/cli.js --version`, or the commit you ran):

**Node version** (`node --version`):

**Brain / model** (e.g. claude / opus, opencode / muse-spark):

**Repro command** (exact flags, target redacted if private):

**Expected**:

**Actual** (what happened instead, with the one number if it was a run):

**Logs** (paste the tail of the run: `runs/<site>/RUN.md`, or the error text):
