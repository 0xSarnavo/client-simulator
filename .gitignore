node_modules/
dist/
runs/
personas/
.DS_Store
.env
.leakdown-state.json
# compat: old state file from client-simulator era (remove after next minor)
# .clientsimulator-state.json
.env.*
coverage/

# everything machine-local lives in local/ — see local/README.md
local/

# local only — the marketing site deploys straight to Railway
website/
runs-backup-*/

# tool-written local state
.*-upload-state.json
.claude/settings.local.json
